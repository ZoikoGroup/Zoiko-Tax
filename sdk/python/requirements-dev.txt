# Development tools for the Python SDK. None of this is a runtime dependency:
# the SDK itself needs only the standard library.
#
# Pinned exactly, transitive dependencies included, because the generator's
# output is committed and checked: a different version is a different file.
# scripts/generate.py refuses to run under any datamodel-code-generator other
# than the one pinned here.
#
#   python -m pip install -r requirements-dev.txt

# The two tools the gate runs.
datamodel-code-generator==0.83.0
mypy==2.3.1

# Their dependencies, as resolved on CPython 3.12.
annotated-types==0.8.0
argcomplete==3.7.2
ast_serialize==0.11.2
black==26.5.1
click==8.5.0
genson==1.4.0
inflect==7.5.0
isort==8.0.1
Jinja2==3.1.6
librt==0.15.0
MarkupSafe==3.0.3
more-itertools==11.1.0
mypy_extensions==1.1.0
packaging==26.3
pathspec==1.1.1
platformdirs==4.11.12
pydantic==2.13.5
pydantic_core==2.46.5
pytokens==0.4.1
PyYAML==6.0.3
typeguard==4.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
